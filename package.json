{
  "name": "pf2e-lite",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "aon": "node scripts/aon-fetch.mjs",
    "compile-data": "node scripts/compile-data.mjs",
    "dev:extension": "node extension/build.mjs --watch",
    "build:extension": "node extension/build.mjs",
    "dev:builder": "npm run dev --prefix builder",
    "build:builder": "npm run build --prefix builder",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.0",
    "eslint": "^9.27.0",
    "prettier": "^3.5.3",
    "typescript": "~5.8.3"
  }
}
